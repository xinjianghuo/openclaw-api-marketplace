# 2026-03-30 - PayPal 集成完成，待部署

## 🕐 10:35 PayPal 支付集成完成

**状态**: Code complete, ready for deployment

---

### 🔄 平台迁移完成（Gumroad → PayPal）

**新架构**:
- PayPal Commerce Platform (中国用户可用)
- Vercel 部署 + KV 持久化
- JWT 许可证系统
- 自动捕获支付并颁发 license

**实现文件**:
- `/api/paypal/create-order` - 创建订单
- `/api/paypal/capture` - 捕获支付并发牌
- `/api/paypal/success` - 成功页面展示 license
- `/api/paypal/webhook` - 异步 webhook 备份
- `/lib/paypal-service.js` - PayPal API 封装
- `/lib/license-manager.js` - 许可证管理 (KV 存储)
- `/public/index.html` - 购买页面

**配置**:
- `.env.local` 已添加: `PAYPAL_CLIENT_ID`, `PAYPAL_CLIENT_SECRET`, `PAYPAL_SANDBOX=true`
- `vercel.json` 路由已配置
- 主 API (`/api/run`) 已切换为 license-manager 验证

---

### 🚀 部署待办（手动步骤）

1. **运行 Vercel 部署** (需登录)：
   ```bash
   cd projects/api-marketplace
   vercel --prod
   ```
   - 获取部署 URL（如 `https://api-marketplace.vercel.app`）

2. **设置环境变量**（Vercel Dashboard → Project Settings → Environment Variables）：
   - `PAYPAL_CLIENT_ID` = `AaHszY2Kn2lF3FeUybv1ax5H7YLJ9kDe-_Djp14KrV95Aj6qhu0drL4hDEdgo5gLJ7KdzfRqGIzpJPuj`
   - `PAYPAL_CLIENT_SECRET` = `EC6-TbKj2WfqpoTNYQv6HQoFbOin7d6QRnEMT5_e0rIXVWbPmU-xkiWmgZStwC-vSe2DAneSEF7S7gLq`
   - `PAYPAL_SANDBOX` = `false` (生产) 或 `true` (测试)
   - `JWT_SECRET` (已存在) = `613002256e8e8b9f7e6c6c89a154c6ed6ac4b338eb5451f79c37a9e255712a42`
   - `VERCEL_KV_REST_API_URL` (已存在)
   - `VERCEL_KV_REST_API_TOKEN` (已存在)

3. **注册 PayPal Webhook** (可选但推荐)：
   ```bash
   node scripts/register-paypal-webhook.js
   ```
   - 设置环境变量 `PAYPAL_WEBHOOK_URL` = `https://your-app.vercel.app/api/paypal/webhook`
   - 脚本返回 `PAYPAL_WEBHOOK_ID` → 填入 Vercel 环境变量

4. **测试购买流程**：
   - 访问 `https://your-app.vercel.app`
   - 点击 "Buy Now with PayPal"
   - 使用 PayPal Sandbox 账号测试（或真实付款）
   - 支付完成后应跳转 success 页面显示 license key

5. **启动 Reddit Show HN**（部署后立即执行）

---

### ⏳ 阻塞状态

**当前阻塞**: 等待部署完成  
**剩余窗口**: Show HN 48小时倒计时（自 08:35 起）  
**优先级**: P0

**一旦部署完成**:
- 立即测试购买
- 启动 Show HN 发布
- 设置 Opportunity Scanner cron